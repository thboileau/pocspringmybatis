<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <mappers>
        <mapper resource="mapper/KikouMapper.xml" />
        <!-- on peut ajouter plusieurs mappers -->
    </mappers>
</configuration>
